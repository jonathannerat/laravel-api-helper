<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Query parameter names
    |--------------------------------------------------------------------------
    |
    | This values specify the names that should be used by the controller to
    | parse the query parameters from the requests. Should be the same as the
    | ones defined in the companion JS package configuration.
    |
    */

    'param_names' => [
        'columns' => '_columns',
        'filters' => '_filters',
        'relationships' => '_relationships',
    ]
];
